import { test } from '@playwright/test';

// Superseded by playwright-blink.spec.ts which drives real Obsidian via CDP.
test.skip('selectionChanged payload is emitted when selection updates', () => {});
